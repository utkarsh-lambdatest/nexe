---
framework: mocha

preRun:
  command:
    - npm i

postMerge:
  pattern:
    - "**/*.spec.ts"
  # strategy for trigerring builds for post-merge

preMerge:
  pattern:
    - "**/*.spec.ts"
    
parallelism: 3


version: 1.0.12
